# -*- mode: snippet; require-final-newline: nil -*-
# name: react-component
# key: react-component
# binding: direct-keybinding
# define
# --
import React from "react";

interface Props {
}

export default function ${1:`(file-name-nondirectory (file-name-sans-extension (buffer-name)))`}(props: Props) {
  return (
    <div className="${1:$(string-inflection-kebab-case-function yas-text)}">
      $0
    </div>
  );
}
