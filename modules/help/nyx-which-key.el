;;; nyx-which-key.el --- which-key -*- lexical-binding: t; -*-
;;; Commentary:
;;; Code:

(use-package which-key
  :ensure t
  :config
  (which-key-mode +1))

(provide 'nyx-which-key)
;;; nyx-which-key.el ends here
